import 'package:project4/models/word.dart';

class modelStatistical {
  String listWord;
  int totalWord;
  int totalQues;
  String time;
  int totalNewWord;
  int totalNewQues;
  int totalOldWord;

  modelStatistical({
        required this.listWord,
        required this.totalWord,
        required this.totalQues,
        required this.time,
        required this.totalNewWord,
        required this.totalNewQues,
        required this.totalOldWord,
      });
}